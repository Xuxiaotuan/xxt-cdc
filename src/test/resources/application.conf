# 测试默认配置：override reference.conf 中的 cluster provider 回 local，
# 避免大多数不涉及 cluster 的测试因为 seed-nodes 为空而卡住。
#
# 需要 cluster 的测试（如 ClusterFormationSpec）应当显式构造 ConfigFactory.parseString(...)
# 并把它放在 withFallback 链最高优先级。
pekko {
  loglevel = "WARN"

  actor {
    provider = "local"
  }
}
