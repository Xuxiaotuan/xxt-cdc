# ADR-0005: 使用 Hash Router 保证同主键顺序

## 决策

使用 `hash(table + primary key) % partitionCount` 将同表同主键事件路由到同一分区。

## 原因

同一主键的更新必须保持顺序，否则可能出现旧事件覆盖新事件。

## 后果

- 不同主键可以并行写入。
- 热点主键仍会串行，这是顺序一致性的必要代价。
- 无主键表不适合作为默认同步对象。
